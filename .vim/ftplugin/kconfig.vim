set tabstop=4           " tab character is 4 spaces wide
set shiftwidth=4        " hitting ">>" will move line by 4 spaces
set noexpandtab         " produce tab when <tab> is hit
set autoindent          " when <enter> is hit, stay in the same column
